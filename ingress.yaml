apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rollouts-demo
  annotations:
    ingress.kubernetes.io/proxy-body-size: 100M
    kubernetes.io/ingress.class: "ingress-nginx-office"  # Updated Ingress class
    ingress.kubernetes.io/app-root: "/"
spec:
  rules:
  - host: rollouts-demo.apps.argoproj.io
    http:
      paths:
      - path: /
        backend:
          service:
            name: rollouts-demo  # Updated syntax for service name (kubernetes v1.19+)
            port:
              number: 80  # Updated syntax for port

